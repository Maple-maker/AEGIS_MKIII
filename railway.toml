[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "gunicorn dashboard_server:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[mounts]]
mountPath = "/app/config"
